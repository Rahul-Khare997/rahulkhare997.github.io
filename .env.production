# Public build-time values. NEXT_PUBLIC_* is inlined into the client bundle by
# design, so nothing here is secret — this is just the proxy's address.
# The Gemini API key lives only as a Cloudflare Worker secret (see worker/).
NEXT_PUBLIC_AI_PROXY_URL=https://gemini-proxy.rahulkhare.workers.dev
