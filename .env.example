# Cloudflare Worker URL for the AI chat proxy (see worker/README.md).
# The Gemini API key is NOT here — it is a Worker secret.
# NEXT_PUBLIC_AI_PROXY_URL=https://gemini-proxy.<subdomain>.workers.dev

# Optional contact-form endpoint (Formspree / Web3Forms / Getform).
# NEXT_PUBLIC_FORM_ENDPOINT=
